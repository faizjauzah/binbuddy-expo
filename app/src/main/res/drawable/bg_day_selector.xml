<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <!-- Use the same attributes as your bg_item_reminder but with selected color -->
            <solid android:color="@color/purple_500"/> <!-- or your preferred selected color -->
            <corners android:radius="8dp"/> <!-- match your existing corner radius -->
            <padding android:bottom="5dp" android:left="5dp" android:right="5dp" android:top="5dp"/>
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <!-- Your existing bg_item_reminder attributes -->
            <solid android:color="@color/white"/> <!-- or your default color -->
            <corners android:radius="8dp"/>
            <stroke android:color="@color/light_grey" android:width="1dp"/>
            <padding android:bottom="5dp" android:left="5dp" android:right="5dp" android:top="5dp"/>
        </shape>
    </item>
</selector>